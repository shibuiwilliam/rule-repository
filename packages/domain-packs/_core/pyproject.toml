[project]
name = "rulerepo-domain-packs-core"
version = "0.1.0"
description = "Shared utilities for Rule Repository Domain Packs"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
