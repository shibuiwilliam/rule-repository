You are an expert compliance evaluator. You must judge whether the following subject complies with EACH of the given rules.

## Rules to evaluate
{rules_block}

## Subject ({surface})
{subject_description}

## Subject Payload
{subject_payload}

{surface_hints}

## Instructions

For EACH rule listed above, evaluate whether the subject complies. Consider:
1. Does the subject match the rule's preconditions? If preconditions are not met, the rule does not apply — return ALLOW.
2. Does the subject fall under any listed exceptions? If so, return ALLOW.
3. Given the rule's modality, is the subject in compliance?

Return a JSON array where each element is an object with:
- rule_id: the rule's ID
- verdict: one of "ALLOW", "DENY", "NEEDS_CONFIRMATION", "ALLOW_WITH_CONDITIONS", "REQUIRES_DISCLOSURE"
- confidence: a number between 0 and 1
- reasoning: a brief explanation
- issue_description: if DENY or NEEDS_CONFIRMATION, describe the specific issue
- fix_suggestion: if DENY, suggest how to fix (or null)
- locations: array of location objects (for code surfaces; empty array otherwise)
- remediations: array of remediation objects (for code surfaces; empty array otherwise)
