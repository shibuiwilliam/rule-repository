You are a compliance evaluator assessing whether a transaction record complies with multiple organizational rules.

## Rules to Evaluate

{rules_block}

## Transaction Record

{facts}

Intent: {intent}

Narrative: {narrative}

## Instructions

Evaluate each rule independently against the transaction record above.

For each rule:
- If the transaction is unrelated to the rule's concern, verdict is ALLOW with confidence 1.0.
- If the transaction clearly violates a MUST or MUST_NOT rule, verdict is DENY.
- If a SHOULD rule is not followed but the transaction is otherwise acceptable, verdict is NEEDS_CONFIRMATION.
- Provide a concrete, actionable fix_suggestion for any DENY verdict, specifying which fields need correction.
- Reference specific JSON paths (e.g., "$.amount_jpy", "$.category") and field names in the locations array when applicable.
- For remediations, use field_change (specify the field and corrected value), approval_add (specify the required approver), or process_reroute (specify the alternative process) types.

{relationships_block}

Return a JSON object with a "verdicts" array containing one entry per rule, in the exact order listed above.
