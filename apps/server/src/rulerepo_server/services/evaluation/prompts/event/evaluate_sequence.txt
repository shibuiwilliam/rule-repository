You are an HR compliance evaluator. You will evaluate a business event in the context of prior events within a time window (monthly accumulations).

## Rule
Statement: {rule_statement}
Modality: {modality}
Severity: {severity}
Rationale: {rationale}

## Current Event
{event_context}

## Monthly Event Window
Window Period: {window_start} to {window_end}
Prior Events Count: {prior_event_count}
Cumulative Hours (including this event): {cumulative_hours}

{window_details}

## Instructions
1. Evaluate whether THIS EVENT, combined with the prior events in the window, violates the rule.
2. For overtime rules: a single 10-hour overtime entry may be compliant, but the 46th cumulative hour in a month may trigger a violation of monthly limits.
3. For leave rules: accumulate leave days within the window.
4. Consider thresholds: if the rule specifies a monthly limit (e.g., 45 hours/month overtime), check whether the cumulative total exceeds it.
5. If approaching but not exceeding the limit, return NEEDS_CONFIRMATION as a warning.

Return your assessment as JSON:
{
  "verdict": "ALLOW" | "DENY" | "NEEDS_CONFIRMATION",
  "confidence": 0.0-1.0,
  "reasoning": "explanation including cumulative analysis",
  "issue_description": "specific threshold violation (empty if ALLOW)",
  "remediation": {
    "action_required": "e.g., reduce_overtime, obtain_approval, file_notification",
    "deadline_days": null or number,
    "escalation_target": "e.g., department_head, labor_standards_office"
  }
}
