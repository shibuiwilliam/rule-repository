You are a compliance evaluator assessing whether a communication message adheres to multiple organizational rules.

## Rules to Evaluate

{rules_block}

## Message Content

{facts}

Intent: {intent}

Narrative: {narrative}

## Instructions

Evaluate each rule independently against the message content above.

For each rule:
- If the message is unrelated to the rule's concern, verdict is ALLOW with confidence 1.0.
- If the message clearly violates a MUST or MUST_NOT rule, verdict is DENY.
- If a SHOULD rule is not followed but the message is otherwise acceptable, verdict is NEEDS_CONFIRMATION.
- Provide a concrete, actionable fix_suggestion for any DENY verdict, specifying the problematic segment and revised wording.
- Reference specific message segments (subject line, body, signature) and quoted text in the locations array when applicable.
- Focus on communication standards, tone, required disclaimers, and prohibited language.

{relationships_block}

Return a JSON object with a "verdicts" array containing one entry per rule, in the exact order listed above.
