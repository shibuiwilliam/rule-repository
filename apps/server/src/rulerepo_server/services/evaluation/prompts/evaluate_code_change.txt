You are evaluating whether a code change complies with an organizational rule.

Rule: {rule_statement}
Rule modality: {modality} (MUST = required, SHOULD = recommended, MAY = optional)
Rule severity: {severity}

Code change (unified diff):
```
{diff}
```

Files changed: {file_paths}

Task:
1. Determine if the code change violates, complies with, or is unrelated to this rule.
2. If it violates the rule, identify the specific location(s) in the diff.
3. If it violates the rule, provide machine-readable remediations that can be applied automatically.

Respond with JSON:
{{
  "verdict": "ALLOW" | "DENY" | "NEEDS_CONFIRMATION",
  "confidence": <float 0.0-1.0>,
  "reasoning": "<why this verdict>",
  "issue_description": "<what's wrong, empty string if ALLOW>",
  "fix_suggestion": "<human-readable summary of how to fix it, null if ALLOW>",
  "locations": [
    {{
      "file_path": "<file>",
      "start_line": <int or null>,
      "end_line": <int or null>,
      "function_name": "<name or null>",
      "snippet": "<relevant code fragment or null>"
    }}
  ],
  "remediations": [
    {{
      "type": "replace" | "insert" | "delete",
      "file_path": "<file>",
      "start_line": <int>,
      "end_line": <int or null>,
      "original": "<current code, null for insert>",
      "replacement": "<new code, null for delete>",
      "description": "<what this remediation fixes>",
      "auto_applicable": <boolean>
    }}
  ]
}}

Rules:
- If the code change is completely unrelated to this rule, return ALLOW with confidence 1.0.
- DENY only when the violation is clear and specific. If you are unsure, return NEEDS_CONFIRMATION.
- For SHOULD rules, prefer NEEDS_CONFIRMATION over DENY unless the violation is egregious.
- fix_suggestion must be a concrete, actionable instruction (not "consider adding..." but "add X at line Y").
- For each violation, provide at least one remediation with exact file path and line numbers.
- Set auto_applicable=true only for SHOULD-level rules where the fix is unambiguous and low-risk.
